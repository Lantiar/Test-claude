# 1.62+ required: 1.46 launches Chromium with --headless=old, which
# this container's build has removed, so every launch fails. Installing
# skyvern pulls playwright backwards and breaks every browser test.
playwright>=1.62
fastapi>=0.115
uvicorn[standard]>=0.30
jinja2>=3.1
python-multipart>=0.0.9
pytest>=8.0
# Agent lane (iCIMS, Ashby, Oracle/Taleo, unknown hosts) and the LLM judge.
# Needs a model configured via LLM_PROVIDER; the DOM lane works without it.
browser-use>=0.13

# Only needed when LLM_PROVIDER=anthropic
anthropic>=0.70
