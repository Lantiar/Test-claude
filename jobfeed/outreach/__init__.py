"""Recruiter outreach: applied -> recruiters -> drafts -> queue -> send -> watch.

Nothing here sends on import, and every entry point defaults to a dry run.
`jobfeed outreach dispatch` needs --send spelled out.
"""
